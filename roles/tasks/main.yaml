- hosts: localhost
 
  tasks:
        - name: "installing httpd"
          package:
              name: "httpd"
              state: present

- name: "web Server start"
  service:
       name: "httpd"
       state: started
       enabled: yes